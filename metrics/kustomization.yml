resources:
  - namespace.yml
  - ingressroute.yml
  - application.yml

generatorOptions:
  disableNameSuffixHash: true

secretGenerator:
  - name: vm-grafana
    namespace: metrics-k8s
    envs:
      - ./.grafana-secret.env
